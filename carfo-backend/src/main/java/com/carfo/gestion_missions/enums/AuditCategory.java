package com.carfo.gestion_missions.enums;

/** Catégorie d'une entrée du journal d'audit (utile pour filtrer dans l'UI). */
public enum AuditCategory {
    AUTH,
    MISSION,
    AFFECTATION,
    AGENT,
    CONFIG,
    OTHER
}
